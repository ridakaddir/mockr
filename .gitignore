# Binaries (Go build output at project root)
/mockr
/mokr
*.exe
*.exe~
*.dll
*.so
*.dylib

# Go build cache
*.test
*.out
coverage.out
coverage.html
/dist/

# Dependency directories
vendor/

# Environment
.env
.env.*
!.env.example

# Task cache
.task/

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Record mode output — stubs and routes captured at runtime should not be committed
# unless you explicitly want to check them in.
examples/record-mode/recorded.toml
examples/record-mode/stubs/

# openapi-generate example — generated output (regenerate with: mockr generate ...)
examples/openapi-generate/mocks/
examples/openapi-generate/mocks-yaml/
examples/openapi-generate/mocks-single/

# Any local mockr config and stubs at the project root (from --init runs)
/mockr.toml
/mockr.yaml
/mockr.yml
/mockr.json
/stubs/
